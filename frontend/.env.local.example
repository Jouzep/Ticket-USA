# Public — used in the browser by EventSource for SSE
NEXT_PUBLIC_API_URL=http://localhost:8000

# Server-only — used by Next.js route handlers to reach FastAPI internally
INTERNAL_API_URL=http://localhost:8000
